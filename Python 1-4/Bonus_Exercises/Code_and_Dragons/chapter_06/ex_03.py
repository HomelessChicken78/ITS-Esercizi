'''Prossimo: Nella stanza successiva, la sfida pretende divisioni sicure con un valore di riserva.

Fontana di Mercurio
Una fontana di mercurio mette alla prova il discernimento: versa soltanto se è sicuro.
Realizza `safe_div(a, b, default=None)` restituendo `default` quando `b == 0`, altrimenti `a/b`
come **float**. Mantieni la firma e placa i test.'''

def safe_div(a: float, b: float, default=None):
    ...